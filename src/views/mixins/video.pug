mixin video(video)
    div
        h4
            //- a(href="/videos/#{video.id}")=video.title  ->  이 기호를 쓰는 방식은 attribute에서는 사용할 수 없다.          
            //- 방법1. a(href="/videos/"+video.id)=video.title    
            //- 방법2.
            a(href=`/videos/${video.id}`)=video.title     


        ul
            li #{video.rating}/5.
            li #{video.comments} comments.
            li Posted #{video.createdAt}.
            li #{video.views} views.