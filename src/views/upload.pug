extends base

block content
    if errorMessage
        span=errorMessage
    form(method="POST" enctype="multipart/form-data")
        //- 모든 input에는 name이 설정되어 있어야 controller에서 req.body.해당name을 사용해서 값을 받아올 수 있다.
        label(for="video") Video File
        input(type="file", accept="video/*", required, id="video", name="video")
        input(placeholder="Title" required type="text" name="title" maxlenth=50)
        input(placeholder="Description" required type="text" name="description" minlength=5)
        input(placeholder="Hashtags, sperated by comma." required type="text" name="hashtags")
        input(type="submit" value="Upload Video")