export const trending = (req, res) => res.send("Home page Videos");
export const watch = (req, res) => res.send("Watch");
export const eidt = (req, res) => res.send("Edit");

// export default는 한 가지만 export 할 수 있다.
// 따라서 여러 개를 export 해야 한다면 위에 처럼 const 앞에 export를 붙인다.
